import React from 'react';
import Button from 'material-ui/Button';

export class Schedule extends React.Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render()  {
    return <Button variant="raised">Default</Button>;
  }

}

export default Schedule;